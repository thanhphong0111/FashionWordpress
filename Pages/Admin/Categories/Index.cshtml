@page
@model Group1_CourseOnline.Pages.Admin.Categories.IndexModel
@{
    Layout = "~/Pages/Shared/_Layout1.cshtml";
    int count = 1;
}
<head>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
</head>
<!-- Style của confirm xóa -->
<style>
    body {
        font-family: 'Varela Round', sans-serif;
    }

    .modal-confirm {
        color: #636363;
        width: 400px;
    }

        .modal-confirm .modal-content {
            padding: 20px;
            border-radius: 5px;
            border: none;
            text-align: center;
            font-size: 14px;
        }

        .modal-confirm .modal-header {
            border-bottom: none;
            position: relative;
        }

        .modal-confirm h4 {
            text-align: center;
            font-size: 26px;
            margin: 30px 0 -10px;
        }

        .modal-confirm .close {
            position: absolute;
            top: -5px;
            right: -2px;
        }

        .modal-confirm .modal-body {
            color: #999;
        }

        .modal-confirm .modal-footer {
            border: none;
            text-align: center;
            border-radius: 5px;
            font-size: 13px;
            padding: 10px 15px 25px;
        }

            .modal-confirm .modal-footer a {
                color: #999;
            }

        .modal-confirm .icon-box {
            width: 80px;
            height: 80px;
            margin: 0 auto;
            border-radius: 50%;
            z-index: 9;
            text-align: center;
            border: 3px solid #f15e5e;
        }

            .modal-confirm .icon-box i {
                color: #f15e5e;
                font-size: 46px;
                display: inline-block;
                margin-top: 13px;
            }

        .modal-confirm .btn, .modal-confirm .btn:active {
            color: #fff;
            border-radius: 4px;
            background: #60c7c1;
            text-decoration: none;
            transition: all 0.4s;
            line-height: normal;
            min-width: 120px;
            border: none;
            min-height: 40px;
            border-radius: 3px;
            margin: 0 5px;
        }

        .modal-confirm .btn-secondary {
            background: #c1c1c1;
        }

            .modal-confirm .btn-secondary:hover, .modal-confirm .btn-secondary:focus {
                background: #a8a8a8;
            }

        .modal-confirm .btn-danger {
            background: #f15e5e;
        }

            .modal-confirm .btn-danger:hover, .modal-confirm .btn-danger:focus {
                background: #ee3535;
            }

    .trigger-btn {
        display: inline-block;
        margin: 100px auto;
    }
</style>
<!-- Hiển thị thông báo AlerBox -->
<div id="AlertContainer" style="position: fixed; top: 50px; right: 40px; z-index: 9999;">
    @if (TempData["AlertMessage"] != null)
    {
        <div id="AlertBox" class="alert alert-dismissible @TempData["AlertType"]" role="alert">
            <strong>@TempData["Notification"]!</strong>
            @TempData["AlertMessage"]
        </div>
    }
</div>


<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Category Course</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="/Admin">Home</a></li>
                        <li class="breadcrumb-item active">Category Course</li>
                    </ol>
                </div>
            </div>

        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <form method="post" asp-page-handler="DeleteCategoriesSelectItem">
        <div class="container-fluid">
            <!-- Popup create category course-->
            <div class="row pb-2 mx-2 justify-content-between">
                <button class="btn btn-success btn-sm py-2 px-4" type="button" data-toggle="modal" data-target="#createCategoryModal">
                    <i class="fas fa-plus"></i>
                </button>
                    <button id="btnDelete" class="btn btn-danger btn-sm py-2 px-4" type="submit" disabled>
                        <i class="fas fa-trash">
                        </i>
                    </button>
            </div>
           
            <div class="row">
                <div class="col-12">
                    <div class="card">

                        <!-- /.card-header -->
                        <div class="card-body">

                            <table id="example1" class="table table-bordered table-striped">
                                <thead>
                                    <tr>
                                            <th> <input type="checkbox" id="selectAllCategory" /></th>
                                        <th>#</th>
                                        <th>Category Name</th>
                                        <th>Description</th>
                                        <th style="width:50px">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.Categories)
                                    {
                                        <tr>
                                                <td width="10px"> <input type="checkbox" name="selectedCategoryId" value="@item.CategoryId" /></td>
                                            <td>@count</td>
                                            <td>@item.CategoryName</td>
                                            <td>@item.Description</td>
                                            <td>
                                                <a asp-route-categoryId="@item.CategoryId" asp-page="Index"><i class="fa fa-edit" style="font-size:16px; margin-left: 10px" data-toggle="modal" data-target="#editCategoryModal" aria-hidden="true"></i></a>
                                                <a data-categoryId="@item.CategoryId" onclick="openDeleteModal(this)"><i class="fa fa-trash" style="color:red; font-size:16px; margin-left: 10px" ; margin-right:10px" aria-hidden="true" data-target="#ConfirmDeleteModal" data-toggle="modal"></i></a>
                                            </td>
                                        </tr>
                                        count++;
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        </form>
    </section>
</div>
<div class="modal fade" id="createCategoryModal" tabindex="-1" role="dialog" aria-labelledby="createCategoryModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="createCategoryModalLabel">Create a course category</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @Html.ValidationSummary()
                <form method="post">
                    <div class="form-group">
                        <label for="categoryName">Course Category Name</label>
                        <input type="text" class="form-control" id="categoryName" placeholder="Enter the course category name" asp-for="Category.CategoryName" required>
                    </div>
                    <div class="form-group">
                        <label for="categoryDescription">Description</label>
                        <textarea class="form-control" id="categoryDescription" rows="5" placeholder="Enter the course description" asp-for="Category.Description"></textarea>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <input type="submit" class="btn btn-primary" value="Save">
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@if (Model.id != 0)
{
<div class="modal show fade" id="editCategoryModal" tabindex="-1" role="dialog" aria-labelledby="editCategoryModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="createCategoryModalLabel">Details of a course category</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @Html.ValidationSummary()
                    <form method="post" asp-route-categoryId="@Model.Category.CategoryId">
                    <div class="form-group">
                        <label for="categoryName">Course Category Name</label>
                        <input type="text" class="form-control" id="categoryName" placeholder="Enter the course category name" asp-for="Category.CategoryName" value="@Model.Category.CategoryName" required>
                    </div>
                    <div class="form-group">
                        <label for="categoryDescription">Description</label>
                        <textarea class="form-control" id="categoryDescription" rows="5" placeholder="Enter the course description" asp-for="Category.Description"> @Model.Category.Description</textarea>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <input type="submit" class="btn btn-primary" value="Save">
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
}

<div id="ConfirmDeleteModal" class="modal fade">
    <div class="modal-dialog modal-confirm">
        <div class="modal-content">
            <form method="post" asp-page="Delete">
                <div class="modal-header flex-column">
                <div class="icon-box">
                    <i class="material-icons">&#xE5CD;</i>
                </div>
                <h4 class="modal-title w-100">Are you sure?</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <div class="modal-body">
                <p>Do you really want to delete these records? This process cannot be undone.</p>
            </div>
            <div class="modal-footer justify-content-center">
                <input type="hidden" id="deleteCategoryId" name="categoryId" />
                <button type="submit" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-danger">Delete</button>
            </div>
            </form>
        </div>
    </div>
</div>     
<script>
    function openDeleteModal(element) {
        var categoryId = element.getAttribute('data-categoryId');
        document.getElementById('deleteCategoryId').value = categoryId;
    }
    $(document).ready(function () {
        $('form').submit(function (e) {
            e.preventDefault();

            var selectedCategoryId = $('input[name="selectedCategoryId"]:checked')
                .map(function () {
                    return parseInt($(this).val()); // Chuyển đổi giá trị sang kiểu số nguyên
                })
                .get();

            if (selectedProductIds.length > 0) {
                var form = $(this);
                var url = '@Url.Page("Admin/Categories")?pagenumber=' + pageNumber;
                var returnUrl = window.location.href; // Lấy đường dẫn trang hiện tại
                $.ajax({
                    type: 'POST',
                    url: url,
                    traditional: true, // Sử dụng kiểu truyền thống để truyền mảng
                    data: { selectedCategoryId: selectedCategoryId },
                    success: function (response) {
                        // Xử lý phản hồi thành công
                        console.log(response);
                        window.location.href = response; // Chuyển hướng đến trang được trả về
                    },
                });
            }
        });
    });

</script>